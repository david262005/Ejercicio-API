// URL base de la API. Vacío = mismo dominio que el frontend (cuando Express sirve /public).
// Si publicas el frontend por separado (p. ej. GitHub Pages), pon aquí la URL del backend:
// window.API_URL = 'https://mi-api.onrender.com';
window.API_URL = '';
